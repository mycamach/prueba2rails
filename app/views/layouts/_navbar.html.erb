<nav class="navbar navbar-dark bg-dark">
  <a class="navbar-brand" href="<%= root_path %>">Index</a>

  <span class="navbar-text">
    <% if user_signed_in? %>
      <%= "¡Bienvenido #{current_user.name}!" %>
    <% else %>
      <a href="<%= user_session_path %>"></a>
    <% end %>
  </span>

  <% if user_signed_in? %>
  <span class="navbar-text">
  Tareas completadas: <%= current_user.tasks.where(completed: true).count %> / <%= current_user.tasks.count %>
  </span>
  <% else %>
    <% link_to root_path %>
  <% end %>

 <span class="navbar-text">
  <% if user_signed_in? %>
    <a class="nav-link" href="<%= destroy_user_session_path%>", data-method='DELETE'> Cerrar sesión</a>
  <% else %>
    <%= 'BIENVENIDO' %>
  <% end %>
 </span>

</nav>
